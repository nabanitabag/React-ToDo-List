import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

ReactDOM.render(<App />, document.getElementById("root"));

//extracted the Input Area, including the <input> and
//<button> elements into a seperate Component called InputArea.

//manage the state of the input element in InputArea.jsx.
//pass the input value back into
//the addItem() function in App.jsx.
